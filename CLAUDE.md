<!-- @ecoma-io portability convention: host instruction files import AGENTS.md
     and stay one line. Claude Code expands `@AGENTS.md` at load time; keep the
     file a regular file (never a symlink) so Windows checkouts resolve it. -->

@AGENTS.md
