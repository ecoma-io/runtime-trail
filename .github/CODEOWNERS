# Files whose change is a legal act rather than a code change. A licence grant
# is irreversible for code already released under it, and the security policy
# is the promise people report vulnerabilities against — so these must never
# land on an approval from someone who could not make that grant.
#
# This file owns itself first: an unowned CODEOWNERS would let a pull request
# edit that entry and exempt its own author in the same change.
#
# Deliberately NOTHING else is listed: this repository has one maintainer, and
# every other path is already gated by the required checks and the merge
# queue. Listing more would add review requirements a one-person project
# cannot satisfy without weakening the ones that matter.
/.github/CODEOWNERS @johnitvn
/LICENSE            @johnitvn
/SECURITY.md        @johnitvn
