# 1.0 gate checklist

> Machine-checkable 1.0 acceptance for `runtime-trail`. Each row names the
> exact gate — a test, a command, or a fixture — that proves the criterion;
> the status is filled from a run on the stated date, never from assumption.
> `passing` = the named gate ran green; `blocked` = a tracked defect or gap
> stops it; `planned` = the phase owning it has not started. Owner documents
> are the architecture authority table in the repository AGENTS.md. Rerun
> the gate — never trust the row.

| #   | 1.0 criterion                                                                                                | Owner                                                 | Gate                                                                                   | Status (2026-09-19)                                                                                                                                                      |
| --- | ------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------- | -------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | OTLP/HTTP + OTLP/gRPC receivers decode traces, logs, metrics                                                 | `system.md`, `telemetry-model.md`                     | `cargo test -p runtime-trail-server --locked` (transport suites, incl. real-SDK e2e)   | passing                                                                                                                                                                  |
| 2   | Ingestion is bounded: queue ceiling, backpressure, exactly one retryable signal                              | `runtime-constraints.md`, `signal.rs`                 | `cargo test -p runtime-trail-telemetry-ingestion --locked` (queue/signal suites)       | passing                                                                                                                                                                  |
| 3   | Export-atomic admission: a retryable saturation admits nothing; retry delivers each record exactly once      | `runtime-constraints.md`, `telemetry-model.md`        | `tests::a_retryable_saturation_rejects_the_whole_export_atomically`                    | blocked — issue #34 (regression test red today; fix in flight)                                                                                                           |
| 4   | Bounded memory driver honours the retention/residency contract                                               | `storage-model.md`                                    | `cargo test -p runtime-trail-storage-memory --locked`                                  | passing                                                                                                                                                                  |
| 5   | File-backed (embedded) driver parity, durability, retention                                                  | `storage-model.md`                                    | `cargo test -p runtime-trail-storage-sqlite --locked` (store-generic suite must exist) | planned — crate is a declarative stub with zero trait methods ([#35](https://github.com/ecoma-io/runtime-trail/issues/35), Phase 3)                                      |
| 6   | Budgeted query: 5-dimension budget, scan accounting, deterministic snapshot cursor                           | `query-model.md`                                      | `cargo test -p runtime-trail-query --locked`                                           | passing                                                                                                                                                                  |
| 7   | `QueryBudget` consume-once / no-`Clone` invariant pinned by test                                             | `query-model.md`                                      | mutation-probe test named in issue #26                                                 | blocked — issue #26                                                                                                                                                      |
| 8   | Correlation: committed strategies (span identity, trace identity, temporal co-activity)                      | `correlation-model.md`                                | `cargo test -p runtime-trail-correlation --locked`                                     | passing                                                                                                                                                                  |
| 9   | Correlation: full taxonomy (parent/child, resource context, exemplar attachment)                             | `correlation-model.md`                                | coverage test over the three strategies                                                | planned — taxonomy pinned at M0, strategies unstarted ([#37](https://github.com/ecoma-io/runtime-trail/issues/37), Phase 2)                                              |
| 10  | One investigation compose flow: subject + logs + metrics + relations + limits                                | `investigation-model.md`                              | `cargo test -p runtime-trail-investigation -p runtime-trail-server --locked`           | passing                                                                                                                                                                  |
| 11  | Evidence fidelity over HTTP JSON: structured values preserved                                                | `investigation-model.md`                              | regression test for the HTTP renderer (Array/KvList must not render `null`)            | blocked — Array/KvList render `null`, scalars truncated ([#39](https://github.com/ecoma-io/runtime-trail/issues/39))                                                     |
| 12  | Loom UI: investigation surfaces + large-trace virtualization + browser regression                            | `scope.md`                                            | Playwright suite: ingest fixture → investigate → select → evidence → relations         | planned — surfaces wired ([#30](https://github.com/ecoma-io/runtime-trail/issues/30)), no browser suite yet ([#38](https://github.com/ecoma-io/runtime-trail/issues/38)) |
| 13  | MCP tools over the Investigation API, budget/limits parity                                                   | `mcp-model.md`                                        | `cargo test -p runtime-trail-mcp --locked` (tools exist and match HTTP)                | planned — crate is declared-only, zero tools ([#36](https://github.com/ecoma-io/runtime-trail/issues/36), Phase 4)                                                       |
| 14  | Distribution: Docker image (non-root, health, OTLP, investigate, SIGTERM drain); desktop wraps the same core | `system.md`                                           | desktop `shells_the_same_core` + Docker smoke under `pnpm verify`                      | passing (verify-local exit 0)                                                                                                                                            |
| 15  | Resource budgets measured and CI-gated                                                                       | `runtime-constraints.md`, `docs/benchmarks/README.md` | `scripts/bench/` scenarios + machine-attached measurements                             | planned — harness measures 3 cases, no gate (Phase 6)                                                                                                                    |
| 16  | Governance gate: format, lint, typecheck, tests, arch, docs links                                            | `boundaries.md`                                       | `scripts/verify-local.sh` + `pnpm arch:canary`                                         | passing (exit 0)                                                                                                                                                         |

Each `blocked`/`planned` row names its issue or owning phase. A row flips to
`passing` only when its gate command runs green on the branch that claims it.
