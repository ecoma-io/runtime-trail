# Matches the org's frontend repositories: a strict, non-hoisted layout with
# automatic peer installation — `@ecoma-io/archkeep`'s optional `vue` peer and
# Loom's peer range resolve this way.
shamefully-hoist=false
strict-peer-dependencies=false
auto-install-peers=true
